- name: Installing packages
  package: 
    name: "{{ item }}"
    state: present
  loop:
  - python3
  - gcc
  - python3-devel

- name: Installing the Dependencies
  pip:
    requirements: requirements.txt
    executable: pip3.9
  args:
    chdir: /app